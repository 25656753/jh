server:
  port: 8080

spring:
  profiles:
    active: home
  datasource:
    data: classpath:data-mysql.sql
    initialization-mode: always
  thymeleaf:
    mode: LEGACYHTML5
    cache: false
    encoding: UTF-8